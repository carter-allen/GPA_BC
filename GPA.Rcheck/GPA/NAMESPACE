useDynLib(GPA)

import( methods, graphics, Rcpp, parallel, shiny, shinyBS, vegan, ggplot2, ggrepel )

export( GPA, assoc, aTest, pTest )

export( show, print, fdr, cov, estimates, se )

export( fitAll, shinyGPA )
